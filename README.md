# ned

`ed` is the standard editor. `ned` is the nonstandard `ed`.

Why is `ned` nonstandard? `ned` is a the result of Weekend-Driven Development (WDD).
In WDD, the programmer tries not to bother with too many things like standards that would slow their roll.

`ned`!
